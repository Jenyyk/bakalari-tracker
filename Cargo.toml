[package]
name = "bakalari-tracker"
version = "0.1.0"
edition = "2021"

[dependencies]
dotenv = "0.15.0"
reqwest = { version = "0.12.8", features = ["blocking"] }
futures-io = "0.3.30"

[target.'cfg(unix)'.dependencies]
daemonize = "0.5.0"
